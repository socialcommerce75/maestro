Context: I am wondering if you play the role of my principal engineer and provide me a setup step set up plan to achieve the goal I have to develop an entire Agentic system and workflow where I as "Chief of Staff" will manage a JIRA KANBAN board and I can assign various tickets to say 3 agents that will act as the core development team building the code. Each Agent will do a specific tasks with following responsibilities.

- Agent 1 is a senior Frontend engineer and only works on frontend code.
- Agent 2 is a senior backend engineer and only works on backend code.
- Agent 3 checks for security and other vulnerabilities introduced in the code or dependent third-party libraries that we use often and is downloaded via npm.
- Each of the Agents 1, 2 and 3, generates a PR once they fix the code and ran associated Unit tests successfully and then notifies a specific agents.
- Agent 1 notifies Agent 4, Agent 2 notifies Agent 5, Agent 3 notifies Agent 6. Each of the agents 4, 5 and 6 are operating at a Lead Engineer level above Senior Engineer and are experts in Frontend, Backend and Security domains respectively.
- Agent 4, 5, 6 each review the corresponding, applies best practices, comments on the PR, and if everything looks good based on best practices and standards, merges the code into the Github branch, which will not be the main branch but a branch forked as per the instructions in the JIRA ticket. The branch will be forked from the Main branch and each forked branch code will remain active until all the Agent 8 closes it out after merging all the code in the Main code branch.
- It is possible that one of the Agents 1 or 2 or 3 may fork a branch in Github first as it depends on who gets to Github first. But, the agentic system workflow should check to see the specific forked branch matching which will be mentioned in the JIRA ticket with the label called - FORK_BRANCH. Whichever agents 1, 2 or 3 gets triggered first, should fork a branch from Main, and begin to work on the code in the forked branch.
- An Agent 7 will be triggered when all Agents 4, 5, and 6 are complete. This agent 7 will run a test suite after each ENV deployment and posting results back to the JIRA ticket. Once Agent 7 competes, the final Agent 8 will do its job and there should be a notification on the Dashboard for me to run Agent 8 manually.
- Agent 8, will perform the job of deploying all the code onto AWS Development environment - ENV1 - for further testing and if all looks good, upon my approval all the code will be moved onto AWS Staging environment - ENV2 - for further testing like load and stress testing and then upon my approval, all the code will be merged into the Main production code branch and shipped to production by deploying to AWS Production Environment - ENV3.
- Dashboard: Lastly I want a dashboard that shows all the Agents, what work they are doing, what ticket they are working on, what branch of code they are working on and each Agent having a status that shows who is FREE and who is BUSY so I can decide to assign the work to different agents. I also want to see which of the PRs cleared, which PRs were flagged for an Agent to perform re-work.
- Each Agent 1, 2 and 3, who has to rework on a failed or flagged PR has to keep that history so I can assess what was the original work done that resulted in a failed PR and should update the JIRA ticket accordingly via Comments so we can keep track of things. Each Agent should update the JIRA ticket with detailed Comments for audibility and clarity so anyone else while I am away can take it over as the Chief of Staff of this entire workflow system.
- The JIRA ticket update loop needs to be explicit per agent. It is a hard requirement baked into each agent's system prompt, not optional. Every state change — work started, PR created, PR flagged, rework started, rework complete, merged, deployed — should write a timestamped JIRA comment. This is my audit trail.
- Use claude-haiku for simpler tasks (JIRA comment updates, branch checks) and claude-sonnet only for actual code work.
- Rollback Mechanism: when Agent 8 deploys to ENV1 and the deployment fails or smoke tests fail, the system needs a defined rollback path — either automated revert or a clear notification to you to intervene.
- Orchestration Layer: I need a central process — a webhook server — that listens to JIRA events (ticket assigned, status changed), routes work to the correct agent, tracks agent state (FREE/BUSY), and manages the event queue. This infrastructure, not an agent, likely makes the whole thing work.
- All secrets will be maintained in a .env file and .gitignore should ensure no such files are checked in or shared over the internet by any process unless it is part of an API request that requires secrets to be passed securely over the internet
- **Merge conflict handling is undefined.** Agents 1, 2, and 3 may all be working in the same forked branch simultaneously. When two of them push code, the second will face a merge conflict. You need a strategy here — either they work in sub-branches and merge into the FORK_BRANCH, or the orchestrator serializes writes, or agents are given conflict resolution instructions.
- Hard requirement: Do not use Axios APIs have been vulnerable and have caused security attacks lately. All third party APIs should be verified for threats and vulnerabilities and only a stable secure and clean API should be used when downloading third party packages.
